server:
  port: 8997

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Zijian_Test?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Ajax2020
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: Analogie_Phonétique_Sémantique

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10086/eureka/